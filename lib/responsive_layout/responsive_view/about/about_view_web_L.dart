import 'package:flutter/material.dart';

class AboutViewWebL extends StatelessWidget {
  const AboutViewWebL({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      color: Colors.amber,
    );
  }
}
