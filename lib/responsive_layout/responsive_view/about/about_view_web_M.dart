import 'package:flutter/material.dart';

class AboutViewWebM extends StatelessWidget {
  const AboutViewWebM({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      color: Colors.blue,
    );
  }
}
