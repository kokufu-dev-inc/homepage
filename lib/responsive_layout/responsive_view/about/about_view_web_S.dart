import 'package:flutter/material.dart';

class AboutViewWebS extends StatelessWidget {
  const AboutViewWebS({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      color: Colors.green,
    );
  }
}
